<?php

/**
 * Implements hook_theme().
 */
function bljo6035_hello_theme($existing, $type, $theme, $path) {
  $variables = [
    'hello' => [
      'variables' => [
        'text' => NULL,
      ],
      'template' => 'hello',
    ],
  ];
  return $variables;
}
