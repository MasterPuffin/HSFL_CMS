<?php

/**
 * Implements hook_theme().
 */
function bljo6035_theme($existing, $type, $theme, $path) {
  $variables = [
    'bljo6035' => [
      'variables' => [
        'source_text' => NULL,
      ],
      'template' => 'hello',
    ],
  ];
  return $variables;
}
